const { createMalEmbed } = require('./mal-embed');
const { createPlayerEmbed } = require('./player-embed');
const { delay } = require('./delay');

module.exports = {
    createMalEmbed,
    createPlayerEmbed,
    delay,
};
